import React from 'react';
import ReactDOM from 'react-dom';
import DashApp from './admin/dashApp';

$(document).ready(function() {
  ReactDOM.render(<DashApp />, document.getElementById('root'));
});
